    action(type="omfwd"
           name="{{output_name}}"
           Target="{{target}}"
           Port="{{port}}"
           Protocol="{{protocol}}"
           Template="{{ out_template }}"
           ZipLevel="{{zip_level}}"
          )
